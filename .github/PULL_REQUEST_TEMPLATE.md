## Summary / 摘要

<!-- Explain the change in English first, then Chinese when useful. -->

## Verification / 验证

- [ ] `./gradlew clean build` passes.
- [ ] `build.gradle`, processed `plugin.yml`, and the JAR filename use the same version.
- [ ] Player-facing messages are updated in both `zh_CN` and `en_US`.
- [ ] Documentation and changelog are updated.
- [ ] No personal configuration, IP/player data, credentials, logs, or build artifacts are included.

## Compatibility / 兼容性

- Paper/Folia versions tested:
- Java version tested:
